spring.application.name=backend
# server.port=${SERVER_PORT}
server.port=8082
# app.cors.allowed-origins=${CROSS_ORIGIN_URL}
app.cors.allowed-origins=http://localhost:5173

# spring.datasource.url=jdbc:mysql://${DB_HOST}:3306/${DB_NAME}
spring.datasource.url=jdbc:mysql://localhost:3306/think_india_db
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
# spring.datasource.username=${DB_USERNAME}
spring.datasource.username=root
# spring.datasource.password=${DB_PASSWORD}
spring.datasource.password=juniorjain11

spring.jpa.show-sql=false
# spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL_AUTO}
spring.jpa.hibernate.ddl-auto=create
spring.jpa.properties.hibernate.format_sql=false

spring.security.oauth2.client.registration.google.client-name=google
spring.security.oauth2.client.registration.google.client-id= 829789089025-smab0b6tai2gjnvq4fbtn91vkq3pe640.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret= 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


# IMPORTANT: This should be your BACKEND port, not frontend
# spring.security.oauth2.client.registration.google.redirect-uri=${GOOGLE_OAUTH_REDIRECT_URI}
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8082/login/oauth2/code/google 

# OAuth2 Provider Configuration
# spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/auth
# spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
# spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v2/userinfo
# spring.security.oauth2.client.provider.google.user-name-attribute=email

# Set max size for a single file to 25MB
# spring.servlet.multipart.max-file-size=25MB
# # Set max size for the entire request to 100MB
# spring.servlet.multipart.max-request-size=100MB
# spring.servlet.multipart.enabled=true

# # Add debug logging
# logging.level.org.springframework.web=DEBUG
# logging.level.com.thinkIndia=DEBUG

# server.forward-headers-strategy=framework
# server.tomcat.remoteip.remote-ip-header=x-forwarded-for